// Este archivo puede ser eliminado si ya no se necesita la funcionalidad de gráficos.
export function useAdminCharts() {
  // No hay lógica de gráficos aquí
  return {}
}

export type ECOption = Record<string, unknown> // Tipo genérico si aún se necesita el tipo exportado
